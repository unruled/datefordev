$("#div_update_search_tool").hide();
$("#result").html("<%= escape_javascript(render("users")) %>");
$("#result").show();

  $('.range-slider').jRange({
      from: 0,
      to: 100,
      step: 1,
      scale: [0,25,50,75,100],
      format: '%s',
      width: 150,
      showLabels: true,
      isRange : true,
      theme: "theme-blue"
  });